# Test dependencies
# Install with: pip install -r requirements-test.txt
# Note: Data science deps (pandas, numpy, scikit-learn, xgboost) are now in requirements.txt

# Testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
